name: Build & Push Docker Image

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "自定义标签 (留空则使用时间戳)"
        required: false
        default: ""

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Build Astro (Production)
        run: pnpm build
        env:
          # 生产环境不使用 BASE_PATH
          BASE_PATH: /

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate timestamp tag
        id: timestamp
        run: echo "tag=$(date +'%Y%m%d-%H%M%S')" >> $GITHUB_OUTPUT

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=${{ github.event.inputs.tag || steps.timestamp.outputs.tag }}
            type=raw,value=latest
            type=sha,prefix=sha-

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Output image info
        run: |
          VERSION_TAG="${{ github.event.inputs.tag || steps.timestamp.outputs.tag }}"
          echo "🎉 Docker 镜像构建完成！"
          echo ""
          echo "📦 推送的标签："
          echo "  - ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${VERSION_TAG}"
          echo "  - ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest"
          echo "  - ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:sha-${GITHUB_SHA::7}"
          echo ""
          echo "🚀 部署命令（推荐使用版本标签）："
          echo "docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${VERSION_TAG}"
          echo "docker run -d -p 80:80 --name myblog ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${VERSION_TAG}"
          echo ""
          echo "💡 提示：使用具体版本标签而非 latest，确保每次部署获取正确版本"
