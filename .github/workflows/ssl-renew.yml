name: SSL Certificate Management

on:
  # 每月 1 号和 15 号凌晨 3 点执行
  schedule:
    - cron: "0 19 1,15 * *" # UTC 19:00 = 北京时间 03:00
  # 支持手动触发
  workflow_dispatch:

env:
  DOMAIN: xkcoding.com
  WWW_DOMAIN: www.xkcoding.com
  EMAIL: 237497819@qq.com
  CERT_PATH: /opt/myblog/certbot/etc/live/xkcoding.com

jobs:
  ssl-manage:
    runs-on: ubuntu-latest

    steps:
      - name: Manage SSL Certificate
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.ALIYUN_HOST }}
          username: ${{ secrets.ALIYUN_USER }}
          key: ${{ secrets.ALIYUN_SSH_KEY }}
          port: ${{ secrets.ALIYUN_PORT || 22 }}
          script: |
            set -e
            DOMAIN="xkcoding.com"
            WWW_DOMAIN="www.xkcoding.com"
            EMAIL="237497819@qq.com"
            CERT_PATH="/opt/myblog/certbot/etc/live/xkcoding.com"

            echo "🔐 检查 SSL 证书状态..."

            # 创建必要目录
            mkdir -p /opt/myblog/dist
            mkdir -p /opt/myblog/certbot/etc
            mkdir -p /opt/myblog/certbot/var

            # 检查证书是否存在
            if [ -d "$CERT_PATH" ]; then
              echo "📜 证书已存在，执行续期检查..."

              # 执行续期
              docker run --rm \
                -v /opt/myblog/certbot/etc:/etc/letsencrypt \
                -v /opt/myblog/certbot/var:/var/lib/letsencrypt \
                -v /opt/myblog/dist:/var/www/html \
                certbot/certbot renew

              echo "✅ 证书续期检查完成"

              # 重启 nginx 加载新证书
              docker restart myblog-nginx 2>/dev/null || true

            else
              echo "🆕 证书不存在，开始初始化申请..."

              # 停止占用 80 端口的容器
              docker stop myblog myblog-nginx nginx-temp 2>/dev/null || true
              docker rm myblog myblog-nginx nginx-temp 2>/dev/null || true

              # 启动临时 nginx 用于验证（使用默认 root 路径）
              docker run -d --name nginx-temp \
                -p 80:80 \
                -v /opt/myblog/dist:/usr/share/nginx/html \
                nginx:alpine

              sleep 3

              # 申请新证书（webroot 写入宿主机目录，nginx 能访问到）
              docker run --rm \
                -v /opt/myblog/certbot/etc:/etc/letsencrypt \
                -v /opt/myblog/certbot/var:/var/lib/letsencrypt \
                -v /opt/myblog/dist:/webroot \
                certbot/certbot certonly \
                --webroot \
                --webroot-path=/webroot \
                --email "$EMAIL" \
                --agree-tos \
                --no-eff-email \
                -d "$DOMAIN" \
                -d "$WWW_DOMAIN"

              # 停止临时 nginx
              docker stop nginx-temp && docker rm nginx-temp

              echo "✅ 证书申请成功！"
              echo ""
              echo "⚠️  请运行 Docker 部署工作流以启用 HTTPS"
            fi

            # 显示证书信息
            echo ""
            echo "📜 当前证书信息："
            docker run --rm \
              -v /opt/myblog/certbot/etc:/etc/letsencrypt:ro \
              certbot/certbot certificates

      - name: Summary
        run: |
          echo "## 🔐 SSL 证书管理" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **时间**: $(date +'%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
          echo "- **服务器**: \`${{ secrets.ALIYUN_HOST }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **域名**: xkcoding.com, www.xkcoding.com" >> $GITHUB_STEP_SUMMARY
